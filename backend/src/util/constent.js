const paymentPlanAmount = {
  Gold: 100,
  Silver: 50,
  Bronze: 10,
};

const paymentPlanDuration = {
  Gold: 30,
  Silver: 30,
  Bronze: 30,
};

module.exports = { paymentPlanAmount, paymentPlanDuration };
